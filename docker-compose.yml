services:
  backend:
    image: ghcr.io/sharifder/khayalkids-backend:latest
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - /var/khayalkids/stories:/app/stories
      - /var/khayalkids/data:/app/data
      - /var/khayalkids/keys:/app/keys
